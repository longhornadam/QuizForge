"""
Archived pointer for the former numerical question prototype.

The production-ready implementation now resides inside the Packager package.
Historical source code lives in `dev/ARCHIVE/numerical_experimental_v1.0.py`.
"""

if __name__ == "__main__":  # pragma: no cover - guard against accidental execution
    raise SystemExit(
        "Numerical prototype archived. See dev/ARCHIVE/numerical_experimental_v1.0.py or the "
        "Packager implementation in Packager/quizforge/."
    )
